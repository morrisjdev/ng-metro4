import { LoadingDisplayDirective } from './loading-display.directive';

describe('LoadingDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
