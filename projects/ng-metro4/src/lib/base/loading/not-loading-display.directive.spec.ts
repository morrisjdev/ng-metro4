import { NotLoadingDisplayDirective } from './not-loading-display.directive';

describe('NotLoadingDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new NotLoadingDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
