import { CreateHashPipe } from './create-hash.pipe';

describe('CreateHashPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateHashPipe();
    expect(pipe).toBeTruthy();
  });
});
