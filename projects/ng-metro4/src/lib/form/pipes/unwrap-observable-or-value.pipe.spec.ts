import { UnwrapObservableOrValuePipe } from './unwrap-observable-or-value.pipe';

describe('UnwrapObservableOrValuePipe', () => {
  it('create an instance', () => {
    const pipe = new UnwrapObservableOrValuePipe();
    expect(pipe).toBeTruthy();
  });
});
