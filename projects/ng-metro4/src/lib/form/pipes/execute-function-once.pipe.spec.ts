import { ExecuteFunctionOncePipe } from './execute-function-once.pipe';

describe('ExecuteFunctionOncePipe', () => {
  it('create an instance', () => {
    const pipe = new ExecuteFunctionOncePipe();
    expect(pipe).toBeTruthy();
  });
});
