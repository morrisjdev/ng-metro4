import { ErrorDisplayPipe } from './error-display.pipe';

describe('ErrorDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
